

// it should have an array and elements in that array must be 
// and elements inside it must be represented as a card with image, title and price
  
export default function productGrid() {
    return (
        <div className="product-grid">
            <div className="product-card">
                  
            </div>
            </div>
    )
}
    
